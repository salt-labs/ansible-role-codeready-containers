---
#########################
# Client
#########################

# Global flag to disable all client tasks (useful for testing)
crc_client: True

# If enabled, performs installation of the CRC client (OpenShift CLI)
crc_client_install: True

# If enabled, removes existing installation of CRC client (OpenShift CLI)
crc_client_remove: False

# User configurable release details

crc_client_release: "4.8.2"
crc_client_release_version: "4.8.2"
crc_client_release_sha: "756b3d1c8bb675b92f112525cc1d2253952b32ba7e5c71dd1b7a4895bc8511e9"
crc_client_release_url: "https://mirror.openshift.com/pub/openshift-v4/clients/ocp"
crc_client_release_name: "openshift-client-linux-{{ crc_client_release_version }}.tar.gz"
